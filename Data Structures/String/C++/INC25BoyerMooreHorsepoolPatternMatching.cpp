#include <bits/stdc++.h>
using namespace std;

void BadMatchTableCreator(string pattern,int patLen, int BadMatchTable[]){

}

void search(string text,string pattern){
	int txtLen = text.length();
	int patLen = pattern.length();
	int BadMatchTable[256];
	for(int i=0;i<patLen;i++){
}

int main(){
	int t;
	cin>>t;
	while(t--){
		string text,pattern;
		cin>>text>>pattern;
		search(text,pattern);
	}
}
